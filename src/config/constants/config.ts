export enum IconConfig {
  DEFAULT_COLOR = '#245558',
  DEFAULT_SIZE = '24',
}

export enum ButtonConfig {
  PRIMARY_COLOR = '#FFFFFF',
  SECONDARY_COLOR = '#245558',
  DEFAULT_SIZE = '16',
}

export enum RatingStarsConfig {
  DEFAULT_STARS_SIZE = 5,
}

export enum InputConfig {
  DEFAULT_ICON_COLOR = '#245558',
  DEFAULT_ICON_SIZE = '20',
}

export enum HeaderConfig {
  CART_ICON_COLOR = '#245558',
  CART_ICON_SIZE = '25',
}

export enum CartConfig {
  DEFAULT_QUANTITY = 1,
}

export enum ProductListConfig {
  DEFAULT_ITEMS_PER_PAGE = 20,
}
